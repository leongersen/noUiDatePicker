{
  "name": "nouidatepicker",
  "version": "1.1.2",
  "author": "leongersen",
  "license": "MIT",
  "scripts": {
    "jshint": "jshint picker.js",
    "format": "prettier picker.js --write --print-width=120",
    "minify": "uglifyjs picker.js --compress --mangle --output picker.min.js",
    "build": "npm run format && npm run minify"
  },
  "devDependencies": {
    "jshint": "^2.13.6",
    "prettier": "^1.19.1",
    "uglify-js": "^3.7.2"
  },
  "files": [
    "picker.css",
    "picker.js",
    "picker.min.js"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/leongersen/noUiDatePicker.git"
  }
}
